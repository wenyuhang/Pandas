<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pandas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".wxapi.App"
        android:allowBackup="true"
        android:icon="@mipmap/logo_ipnda"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".personal.LoginActivity" />
        <activity android:name=".personal.RegisterActivity" />
        <activity android:name=".personal.SetActivity" />
        <activity android:name=".personal.AboutActivity" />
        <activity android:name=".personal.PersonalCenterActivity" />
        <activity android:name=".personal.CollectionActivity" />
        <activity android:name=".personal.UserFeedbackActivity"></activity>
        <activity android:name=".personal.UpdateActivity"></activity>
        <activity android:name=".personal.HistoryActivity"></activity>
        <activity android:name=".personal.homeinteractive.InteractiveMainActivity"></activity>
        <activity android:name=".personal.PersonalXinActivity"></activity>
    </application>

</manifest>